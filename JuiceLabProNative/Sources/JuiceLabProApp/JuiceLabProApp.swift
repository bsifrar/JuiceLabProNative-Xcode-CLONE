import SwiftUI

@main
struct JuiceLabProApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Hello from JuiceLabPro Native!")
                .font(.largeTitle)
                .padding()
                .frame(minWidth: 600, minHeight: 400)
        }
    }
}
